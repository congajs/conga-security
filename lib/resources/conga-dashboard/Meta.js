// local libs
const package = require('../../../package.json');

module.exports = {

    id: 'security',
    name: 'Security',
    description: package.description,
    bundle: package.name,
    version: package.version,
    icon: 'shield'

};
